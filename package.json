{
  "devDependencies": {
    "lerna": "^8.1.2",
    "react-scripts": "^5.0.1"
  },
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "lerna run start",
    "new-version": "lerna new-version --conventional-commits --yes",
    "test": "lerna run test",
    "diff": "diff lerna"
  }
}
